import { Time } from '@angular/common';

export class Todo  {
    id: number;
    title: string;
    completed : boolean;
    description: string;
    date: string;
    time: string;
    view: boolean;
}

